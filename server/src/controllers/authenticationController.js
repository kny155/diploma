const authenticationController = {
    registration: async ctx => {
		ctx.body = {};
	},
	login: async ctx => {
		ctx.body = {};
	},
    relogin: async ctx => {
		ctx.body = {};
	},
};

export default authenticationController;
